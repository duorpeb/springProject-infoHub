<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace = "com.myproject.www.repository.LikeDAO">
	<select id="checkDuplicLikeNo" resultType="int">
		select count(user_id) from likes where board_bno = #{boardBno} and user_id = #{userId} 
	</select>
	
	<insert id="insertLike">
		insert into likes(user_id, board_bno) values(#{userId}, #{boardBno})
	</insert>
	
	<delete id="deleteLike">
		delete from likes where board_bno = #{boardBno} and user_id = #{userId}
	</delete>

</mapper>